{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}/out/extension.js",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "npm: compile"
    }
  ]
}
